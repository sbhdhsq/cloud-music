<!--<view class="container" hidden="{{!isLyricShow}}">
  {{lyric}}
</view> -->

<scroll-view hidden="{{!isLyricShow}}" class="lyric-scroll" scroll-y>
  <view class="lyric-panel">
    <block wx:for="{{lyrics}}" wx:key="index">
      <view class="lyric">
        {{item}}
      </view>
    </block>
  </view>
</scroll-view>